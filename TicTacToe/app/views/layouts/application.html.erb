<!DOCTYPE html>
<html>
<head>
  <title>TicTacToe</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <script src="http://code.jquery.com/jquery-1.10.2.js"></script>
    <script src="http://code.jquery.com/ui/1.11.0/jquery-ui.js"></script>
  <%= javascript_include_tag "application" %>

  <link href='http://fonts.googleapis.com/css?family=Press+Start+2P' rel='stylesheet' type='text/css'>
  <%= csrf_meta_tags %>
</head>
<body>
<header><div>Professor Falkens World of Tic-Tac-Toe</div></header>  
  <nav>
    <ul>
      <% if current_user %>
        <li id="left"><%= link_to 'New Game', new_tic_tac_toe_game_path %></li>
        <li> <%= link_to current_user.username, user_path(current_user.id) %></li>
        <li><%= link_to 'Logout', logout_path, method: :delete, data: {confirm: "Are you sure?"} %></li>
      <% else %>
        <li><%= link_to 'Please login', login_path %></li> 
        <li><%= link_to 'Signup', signup_path %></li> 
      <% end %>
    </ul>
  </nav>
  <% flash.each do |name, message| %>
    <div class="flash-message flash-message-<%= name %>">
    <%= message %>
    </div>
  <% end %>

  <%= yield %>

</body>
</html>
